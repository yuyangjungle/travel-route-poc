# 项目协作规则

状态：生效。  
最后更新：2026-09-22

## 项目目标

验证确定性搜索是否能联合优化亚洲多目的地旅行的目的地组合、顺序、日期、停留和返程机场，并提供有价值、可解释的取舍方案。详见 [PRD](docs/product/prd.md)。

## 当前里程碑与授权

M9-001 已按用户授权完成产品开发与公开交付（见 [交付报告](docs/m9-results.md)）；本轮停止，不自动推进下一里程碑。交付包括升级 Streamlit、同一确定性核心的 FastAPI/静态 Web 演示、GitHub 发布和 Vercel 公开部署。服务端可直接使用用户已提供的 DeepSeek 密钥，严禁将密钥写入前端、仓库、日志、截图或下载结果。公开演示不等于生产服务承诺；外部航班 API、预订、支付、账户、数据库与微服务仍不在范围内。

M0–M8-002 已完成；冻结 v1 真实模型评估判定为 C，结论保留。M9 的 `m9-intent-v2` 独立维护空值、假设与确认语义，尚未通过独立可靠性评估，真实用户验证仍延期。LLM 只能产生意图草稿；草稿经本地校验、人工补全与确认、`validate_request` 复核后才可搜索。搜索、可行性、评分和 Pareto 仍完全位于 `travel_core`。

后续用户明确授权某个里程碑后，在授权范围内自主完成可逆工作，不为日常编辑、修复或检查重复请求许可。里程碑状态改变时同步 README 和 PRD。

## 文档权威与工作流程

1. 开始工作前阅读本文件、PRD，以及任务涉及的技术文档和 ADR。
2. 产品范围以 PRD 为准；字段和时间口径以数据模型为准；评分与搜索语义以算法文档为准；决策理由以 ADR 为准。
3. 发现冲突时先指出并修订，不把冲突作为自行扩大产品范围的依据。
4. 未决假设登记在 PRD，引用其 A-xx 编号，区分事实、暂定参数和验证结果。
5. 产品行为变更同步 PRD；模型变更同步数据模型和算法；重要技术取舍新增或取代 ADR。
6. 交付时说明改动、验证证据、限制与下一步。不得声称未运行的检查通过。

## 工程原则

- 硬约束由确定性逻辑验证；不得依赖 LLM 判断。
- 相同输入、数据版本、评分版本、搜索配置应复现相同结果与排序。
- 先用小规模完整搜索建立正确性基准，再根据测量优化。
- 只在具有相同后续可行选择的状态间做安全支配剪枝；不能只按机场合并状态。
- 标识数据来源、报价口径和覆盖边界；缺数据不等于无航班。
- 精确结果的最优性仅限已加载数据与声明的离散模型。近似结果不得标为全局最优。
- 无解时不静默放宽预算、日期、必去地点或转机限制。
- 保持模块简单；展示层不复制搜索、计价或评分逻辑。
- 描述性目的地信息不得进入优化、约束或评分；相对解释只能使用本轮实际展示方案和可复算差值。
- LLM 不得读取 FlightOffer、生成路线或绕过 TripRequest 校验；API 不可用时必须保留手动模式。
- AI 输入评估不得把 schema 合法等同于语义正确；冻结语料的修改必须提升版本和内容锁，针对结果调参后必须使用独立 holdout。
- M9 修改仅限已授权的产品层、展示层、数据适配和交付；不得改写 `travel_core`、M1 fixture、M2 oracle、M5 模拟数据或 M7/M8 冻结边界以迎合产品结果。新意图契约独立版本化，不将 v2 组件测试当成可靠性通过。
- 描述目录 40 个地点与 Web 搜索子集的 18 个地点/264 条报价分别标识；缺少报价不能解释为现实无航班。发布状态以实际可访问 URL 与端到端验证为准。

## 未来代码约定

以下约定适用于本轮验证工具及后续获授权的实现。

- 使用 Python，函数、变量、模块用 `snake_case`，类型用 `PascalCase`。
- Web 浏览器层采用简单 HTML/CSS/JavaScript，负责编辑和展示；校验、调用模型与路线计算在服务端完成。模型文本按文本渲染，不作为 HTML 执行。
- 核心模型和函数写类型标注；公共边界有清晰的校验和错误类型。
- 金额用整数分，币种显式记录；第一版仅接受 CNY。
- 起降时间带时区偏移，机场保留 IANA 时区；持续时间统一用分钟。
- 评分使用固定参数版本与可复现计算，避免根据本轮候选极值动态归一化。
- ID 稳定且显式；JSON 字段名与数据模型一致。
- 用户文档与注释优先中文，标识符英文；注释解释原因和约束，不重复代码。
- 核心逻辑与文件读写、UI 分离；不依赖未声明的全局状态。
- 模拟数据固定或使用记录的种子，不在搜索过程中随机报价。
- M1 环境为 Python 3.12 和 requirements.txt 锁定的 tzdata；使用 `py -3.12`，避免误用本机默认 Python 3.8。
- 基准运行器必须只读。修改样本或预期时，先手工核对产品意义和数值，再显式更新内容锁；不得用被测工具的输出批量覆盖预期。
- 依赖在确有需要时引入，首次实施时记录版本和本地运行步骤。

## 验证要求

- 核心搜索必须验证：小规模独立穷举对照、约束检查、跨日与时区、无解与数据缺失、Pareto 筛选、增加一站更便宜的反例。
- M2 使用 benchmark_tools.run_m2 对照完整可行集和 Pareto 集。oracle 不调用 travel_core 的搜索、评分或约束逻辑；共享输入校验和时区解析是明确的独立性边界。
- 正常检查命令：py -3.12 -m unittest discover -s tests -v。不要为了通过检查改写 M1 的固定报价、见证预期或内容锁。
- UI 冒烟测试使用 Streamlit AppTest；启动真实服务后还要检查默认推荐、无解状态和浏览器控制台。演示配置必须固定 offer_ids，保证截图和用户试用可复现。
- Web 接口须覆盖未确认拒绝、非法输入、密钥不回传、缺失信息、模型不可用回退和固定场景；发布时核验桌面/移动端完整旅程。提供者变更保持来源、日期口径、整数金额与优化/描述分离。
- Pilot 记录不自动上传，不添加个人信息字段；产品遥测和主观反馈保持分区。变更字段时同步用户测试手册与指标文档。
- M4-001 开始首名参与者后，不在同一批次中途改变任务、问题或门槛；严重技术故障修复后记录版本并决定是否重做受影响任务。
- 用户研究分析按 Evidence、Opinion、Decision 分层；报告分子/分母、反例和缺失数据，不用单句评价证明产品成功。
- 性能优化前后记录规模、配置、耗时和解质量，不用单一幸运案例证明性能。
- 文档修改进行链接、术语、范围、需求到模型/算法映射检查；不为文案编辑编写无意义测试。

## 未来多代理协作

按用户或适用的协作指令启用多代理；本轮允许按独立文件与明确接口分工。授权不延伸到目标以外的功能。

- 每项任务声明目标、输入、允许编辑的文件、不可变接口、验收方法和依赖。
- 避免多个代理同时编辑同一文件；共享模型或接口由协调者先确定。
- 不覆盖他人未完成的修改，不擅自重置工作区。
- 交接记录变更文件、关键决定、验证命令/结果和未解决问题。
- 协调者负责集成后的一致性检查；子任务完成不等于整体完成。

## 避免事项

不做支付、登录、全球覆盖、实时抓取、自动拼接自行转机、酒店/活动联合优化、签证规则引擎、LLM 路线代理或微服务。不把模拟报价当可购买报价，不把机票便宜描述成旅行总费用最低。M9 的公开演示部署已获授权，但不得据此扩展数据库、生产运维体系或商业服务承诺。

